bin/
obj/
.azure/
local.settings.json
*.user
.vs/

# Build/publish artifacts
publish/
publish.zip


publish-out/


src/package.zip

artifact.zip
.webhook-uri.txt

# Smoke-test PKI (private keys, leaf PFX) — public root is committed into
# infra/main.parameters.json so the deployed Function Apps trust it, but
# the .key/.pfx files generated locally MUST never enter source control.
client/tests/smoke-pki/

artifacts/

# IntuneWipeClient icon build scratch
client/intune-win32-package/tools/build/
